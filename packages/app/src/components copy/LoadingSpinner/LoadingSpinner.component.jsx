import React from "react";

const LoadingSpinner = () => (
    <div
        className="m-auto spinner-border text-info loading-page-spinner"
        role="status"
    >
        <span className="sr-only mr-3"></span>
    </div>
);

export default LoadingSpinner;
